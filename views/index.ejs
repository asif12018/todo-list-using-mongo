<!-- <.% %.> without the dot its know as scriptlet tag and its use to 
    add control flow js code in ejs file. its formula is to use it on every none html line-->


<%- include('header') -%>

    <div class="box" id="heading">
        <h1>
            <%= listTitle %>
        </h1>
    </div>

    <div class="box">

        <%for(let item of newItems){%>
            <form action="/delete" method="post">
                <div class="item">
                    <input name="checkbox" onchange="this.form.submit()" type="checkbox" value="<%=item._id%>">
                    <p>
                        <%= item.name %>
                    </p>
                </div>
            </form>
        <%}%>
      

       

                <form action="/" method="post" class="item">
                    <input type="text" name="newItem" placeholder="New Item" autocomplete="off">
                    <button type="submit" name="list" value=<%=listTitle %> >+</button>
                </form>


    </div>

    <%- include('footer') -%>